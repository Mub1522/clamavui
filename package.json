{
  "name": "clamav-ui",
  "private": true,
  "scripts": {
    "dev": "concurrently -n backend,frontend -c red,cyan \"pnpm --filter backend dev\" \"pnpm --filter frontend dev\"",
    "build": "pnpm --filter frontend build",
    "start": "pnpm --filter backend start"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
